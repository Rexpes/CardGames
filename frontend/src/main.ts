import { createApp } from 'vue'
import Vue from 'vue'
import CardGames from './App.vue'
import '/src/assets/css/style.css'
import router from './router'

createApp(CardGames).use(router).use(router).use(router).mount('#app')